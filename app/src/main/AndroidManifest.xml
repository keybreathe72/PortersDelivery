<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.portersdelivery">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".HK3to"
            android:label="@string/title_activity_h_k3to"></activity>
        <activity android:name=".NotAvailable" />
        <activity
            android:name=".HK2to"
            android:label="@string/title_activity_h_k2to" />
        <activity
            android:name=".MongKoK"
            android:label="@string/title_activity_mong_ko_k" />
        <activity android:name=".HKrestaurant3" />
        <activity android:name=".HKrestaurant2" />
        <activity android:name=".HKrestaurant" />
        <activity android:name=".Frestaurant" />
        <activity android:name=".Adessert" />
        <activity android:name=".Agatha" />
        <activity android:name=".StarOcean" />
        <activity android:name=".vivo" />
        <activity android:name=".samsung" />
        <activity android:name=".realme" />
        <activity android:name=".poco" />
        <activity android:name=".apple" />
        <activity android:name=".HomePage" />
        <activity
            android:name=".MainActivity2"
            android:label="@string/title_activity_main2" />
        <activity android:name=".Ordering" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Login" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>